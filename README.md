# Node Jenkins App

This is a simple Node.js application deployed using Jenkins and AWS EC2.

## Steps
1. Install dependencies: npm install
2. Run app: npm start
3. Open browser: http://localhost:3000